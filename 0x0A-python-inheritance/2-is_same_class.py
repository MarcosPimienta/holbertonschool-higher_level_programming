#!/usr/bin/python3
"""Returns true if its the same class"""


def is_same_class(obj, a_class):
    """Returns true if its the same class"""

    if type(obj) is a_class:
        return (True)
    else:
        return(False)
